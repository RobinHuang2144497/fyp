# fyp
## First of all, we need to vertically merge multiple obtained Excel tables through the Python file named "excel merge" and obtain the result. Then, through the Python code named "final merge", we merge another file with the previous one to obtain the data that can be fine - tuned for the LLM. Regarding the files named "stock bar" and "East Money stock bar", they are crawler codes for extracting comments from East Money. However, currently, it is impossible to crawl all the data. It seems that there is an anti - crawling mechanism. Therefore, we need to continue learning and improve these two crawler codes to obtain all the comment data.
